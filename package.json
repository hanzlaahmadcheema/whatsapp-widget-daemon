{
  "name": "whatsapp-widget-daemon",
  "version": "1.0.0",
  "description": "Lightweight event-driven background daemon for WhatsApp Web integration with desktop widgets",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "cli": "tsx src/cli.ts",
    "daemon": "./whatsapp-daemon.sh",
    "test": "tsx test/identity-normalization.test.ts && tsx test/reply-quoting.test.ts && tsx test/chat-identity-sync.test.ts && tsx test/display-name-priority.test.ts && tsx test/identity-routing.test.ts && tsx test/saved-contact-priority.test.ts",
    "lint": "tsc --noEmit",
    "clean": "rm -rf ~/.config/whatsapp-widget-daemon/session",
    "fresh": "npm run clean && npm run build && node dist/index.js",
    "stop": "pkill -f 'node dist/index.js' || true"
  },
  "keywords": [
    "whatsapp",
    "daemon",
    "ipc",
    "desktop-widget"
  ],
  "author": "Hanzla Ahmad",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.12",
    "pino": "^9.6.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "@types/qrcode-terminal": "^0.12.2",
    "nodemon": "^3.1.14",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
